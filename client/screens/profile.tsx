import { View, _Text } from 'react-native';
import _Button from '../components/control/button';
import _TextInput from '../components/control/textinput';

const ProfileScreen = (props: any, {navigation}:any) => {
    /*
    Daniyal: Add all content for the single page view here,
    If you need to make reusable components, create a folder
    in the components folder named "profile" and add your component files there
    */
    return (
    <View>
        
    </View>
    );
};

export default ProfileScreen;