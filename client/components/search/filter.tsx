import { View, _Text } from 'react-native';

const SearchFilter = (props: any, {navigation}:any) => {
    /*
    Daniyal: This component is where you will add the filtering that
    is used back on the explore and match screens. Add this component to
    both of those screens.
    */
    return (
    <View>
        
    </View>
    );
};

export default SearchFilter;